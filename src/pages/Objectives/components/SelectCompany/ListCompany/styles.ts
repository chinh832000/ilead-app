import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  imgTag: {
    height: 20,
    width: 20,
    borderRadius: 99,
    marginRight: 5,
  },

  textTag: {
    fontSize: 12,
    fontWeight: 600,
  },
});
